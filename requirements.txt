# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.7.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.30
alembic>=1.13.0
psycopg2-binary>=2.9.9   # PostgreSQL driver (production)

# Security
# bcrypt is used directly: passlib 1.7.4 reads the removed `bcrypt.__about__`
# and fails outright against bcrypt >= 5.
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0

# ML / data
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
torch>=2.2.0

# Nutrition coach. Gemini is called over its REST API with httpx rather than
# the SDK: the failover design classifies on HTTP status, and reading the
# status directly is more precise than mapping SDK exception types.
# Entirely optional at runtime -- with no key the coach falls back to rules.
httpx>=0.27.0

# Config
python-dotenv>=1.0.0
